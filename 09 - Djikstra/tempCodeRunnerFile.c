    // if (start > 266 || start < 1 || end > 266 || end < 1) {
    //     printf("Can only select indexes from 1 to %d!", numOfPlaces);
    //     printf("%d %d", start, end);
    //     return 0;
    // }

    // if (start == end) {
    //     printf("Start and end cannot be the same!");
    //     return 0;
    // }